spring:
  application:
    name: notification

  mail:
    host: smtp.gmail.com
    port: 587
    username: projetoaccenture26@gmail.com
    password: ${SMTP_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true